---
page_title: "{{.Name}} {{.Type}} - {{.RenderedProviderName}}"
subcategory: ""
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{.Name}} ({{.Type}})

A Role Binding grants the permissions defined in a role to one or more users, groups, or service accounts. This is how you assign access rights in GameFabric's role-based access control (RBAC) system.

Note that service accounts are specified in the `users` list using their service account email.

For details check the <a href="https://docs.gamefabric.com/multiplayer-servers/api/guide#rbac">GameFabric documentation</a>.


## Example Usage

This example creates a role binding that grants a role to both a group and a service account.

{{ tffile "examples/resources/gamefabric_role_binding/single.tf" }}


{{ .SchemaMarkdown | trimspace }}
{{- if or .HasImport .HasImportIDConfig .HasImportIdentityConfig }}

## Import

Import is supported using the following syntax:
{{- end }}

{{- if .HasImportIDConfig }}

In Terraform v1.5.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `id` attribute, for example:

{{tffile .ImportIDConfigFile }}
{{- end }}
{{- if .HasImport }}

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

{{codefile "shell" .ImportFile }}
{{- end }}